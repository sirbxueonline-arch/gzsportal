#
# Supabase Postgres (Prisma)
#
# IMPORTANT (Vercel/IPv4): Use Supabase "Session pooler" connection string.
# The pooler username MUST include your project ref, like: postgres.<project_ref>
#
# Example:
# DATABASE_URL=postgresql://postgres.<project_ref>:<url-encoded-password>@aws-0-<region>.pooler.supabase.com:5432/postgres
#
DATABASE_URL=

#
# Supabase Project
#
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

#
# App Security
#
# 32-byte base64, generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=

# Comma-separated admin emails (case-insensitive).
ADMIN_EMAILS=
